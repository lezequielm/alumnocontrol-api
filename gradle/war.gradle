apply plugin: "war"

bootWar {
    mainClassName = "com.softstonesolutions.alumnocontrol.AlumnocontrolApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
